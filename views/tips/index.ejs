

<ul>
    <% for (var i in tips) { %>
        <% var tip = tips[i]; %>

        <li>
	
	<% var accepted = tip.accepted; %>
	<% var isAdmin = session.user && session.user.isAdmin; %>
	<% var isQuizAuthor = session.user && quizAuthorId && quizAuthorId === session.user.id; %>
	<% var isTipAuthor = session.user && tip.AuthorId && tip.AuthorId === session.user.id; %>

	<%= tip.text %>	
	
	<% if (accepted || isAdmin || isQuizAuthor ||isTipAuthor) { %>
	<span id="autor">(<%=tip.Author ? session.user.username : "Anónimo"%>)</span>

	<li>
		
		<% if (!accepted && (isAdmin || isQuizAuthor || isTipAuthor)) { %>
			<a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>/accept?_method=PUT"><button>Aceptar</button></a>
		<% } %>

		<% if (isAdmin || isQuizAuthor || isTipAuthor) { %>
			<a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>?_method=DELETE"><button>Borrar</button></a>
		<% } %>
	</li>

        <% } %>
           
    <% } %>
	</li>
</ul>
